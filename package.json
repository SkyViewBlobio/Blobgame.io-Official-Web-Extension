{
  "name": "blobio-web-script",
  "version": "0.2.87",
  "private": true,
  "author": "SkyView",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/SkyViewBlobio/Blobgame.io-Official-Web-Extension.git"
  },
  "scripts": {
    "build": "node scripts/build.js --channel=public",
    "build:public": "node scripts/build.js --channel=public",
    "build:beta": "node scripts/build.js --channel=beta",
    "check:structure": "node scripts/check-source-health.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
