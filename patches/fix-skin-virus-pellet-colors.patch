diff -ruN '--exclude=node_modules' blobfix-orig/Blobgameio-extension-BETA-main/dist/blobio-extension.bundle.js blobfix-work/Blobgameio-extension-BETA-main/dist/blobio-extension.bundle.js
--- blobfix-orig/Blobgameio-extension-BETA-main/dist/blobio-extension.bundle.js	2026-07-06 19:26:04.000000000 +0000
+++ blobfix-work/Blobgameio-extension-BETA-main/dist/blobio-extension.bundle.js	2026-07-06 19:56:25.889428767 +0000
@@ -2809,9 +2809,9 @@
         state.markers.normalColorMarks += 1;
       }
       return {
-        d: skin ? 1 : color.d,
-        c: skin ? 1 : color.c,
-        b: skin ? 1 : color.b,
+        d: color.d,
+        c: color.c,
+        b: color.b,
         a: skin ? SELF_SKIN_MARK_ALPHA : SELF_MARK_ALPHA
       };
     }
@@ -3448,7 +3448,7 @@
         "  bool blobioGlowCell = blobioSelfFull || blobioSelfSkin;",
         "  if (blobioGlowCell) {",
         "    if (blobioSelfSkin) { blobioTint.rgb = vec3(1.0); }",
-        "    blobioTint.a = blobioSelfSkin ? blobioTex.a * blobioCellAlpha : blobioDefaultAlpha;",
+        "    blobioTint.a = blobioSelfSkin ? blobioTex.a : blobioDefaultAlpha;",
         "    gl_FragColor = vec4(blobioTint.rgb * blobioTex.rgb, blobioTint.a);",
         "  } else {",
         "    gl_FragColor = blobioTint * blobioTex;",
@@ -3515,11 +3515,11 @@
       );
       patched = patched.replace(
         /\* texColor \* v_color;/,
-        "* texColor * (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color);"
+        "* texColor * (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, blobioSelfSkin ? 1.0 : clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color);"
       );
       patched = patched.replace(
         /\* v_color \* texColor;/,
-        "* (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color) * texColor;"
+        "* (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, blobioSelfSkin ? 1.0 : clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color) * texColor;"
       );
       if (patched !== source) {
         state.shader.legacyBorderPatches += 1;
@@ -4024,6 +4024,8 @@
   var FOOD_CASE_RE = /case 2:case 5:case 0:/;
   var FOOD_BRANCH_HOOK = "if(g.c&&g.c.M==2&&$wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.food(g)}";
   var VIRUS_BRANCH_RE = /case 4:case 3:if\(g\.q\)\{if\(g\.P\)\{h=g\.P;([$A-Za-z_][$\w]*)\(\);([$A-Za-z_][$\w]*)\(a\.c,g\.K\);([$A-Za-z_][$\w]*)\(a\.c,h,g\.R-g\.M,g\.S-g\.M,g\.N,g\.N\)\}\}else\{\1\(\);\2\(a\.c,g\.K\);\3\(a\.c,(\(?a\.[$A-Za-z_][$\w]*\)?),g\.R-g\.M,g\.S-g\.M,g\.N,g\.N\)\}break;/;
+  var VIRUS_CASE_RE = /case 4:case 3:/;
+  var VIRUS_CASE_HOOK = "if(g&&g.K&&$wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.virus(g)}";
   var FALLBACK_RENDER_RE = /function ([$A-Za-z_][$\w]*)\(a,b\)\{var c;if\(b\.q\)\{c=b\.P;([$A-Za-z_][$\w]*)\(\);([$A-Za-z_][$\w]*)\(a\.c,b\.K\);([$A-Za-z_][$\w]*)\(a\.c,c,b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}else if\(b\.P\)\{\3\(a\.c,b\.K\);\4\(a\.c,b\.P,b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}else\{b\.K\.a=0\.75;\3\(a\.c,b\.K\);\4\(a\.c,([$A-Za-z_][$\w]*),b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}\}/;
   var PAGE_HOOK = "__BlobCellColorizer";
   var STATE_KEY = "__blobCellColorState";
@@ -4648,9 +4650,12 @@
       const foodDrawSeen = FOOD_DRAW_RE.test(patched);
       const foodAlphaSeen = FOOD_ALPHA_RE.test(patched);
       let virusPatched = false;
+      let virusPatchMode = "none";
       let fallbackFoodPatched = false;
       let fallbackVirusPatched = false;
-      const virusBranchSeen = VIRUS_BRANCH_RE.test(patched);
+      const exactVirusBranchSeen = VIRUS_BRANCH_RE.test(patched);
+      const virusCaseSeen = VIRUS_CASE_RE.test(patched);
+      const virusBranchSeen = exactVirusBranchSeen || virusCaseSeen;
       if (!patched.includes("$wnd.__BlobCellColorizer.foodBranch(g)") && foodDrawSeen) {
         patched = patched.replace(FOOD_DRAW_RE, (match, setColor, drawRegion, foodTexture) => {
           foodPatched = true;
@@ -4670,10 +4675,11 @@
           return match + FOOD_BRANCH_HOOK;
         });
       }
-      if (virusBranchSeen) {
-        patched = patched.replace(VIRUS_BRANCH_RE, (match, initDrawState, setColor, drawRegion, virusTexture) => {
+      if (!patched.includes("$wnd.__BlobCellColorizer.virus(g)") && virusCaseSeen) {
+        patched = patched.replace(VIRUS_CASE_RE, (match) => {
           virusPatched = true;
-          return `case 4:case 3:if($wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.virus(g)}if(g.q){if(g.P){h=g.P;${initDrawState}();${setColor}(a.c,g.K);${drawRegion}(a.c,h,g.R-g.M,g.S-g.M,g.N,g.N)}}else{${initDrawState}();${setColor}(a.c,g.K);${drawRegion}(a.c,${virusTexture},g.R-g.M,g.S-g.M,g.N,g.N)}break;`;
+          virusPatchMode = exactVirusBranchSeen ? "exact-case-label" : "generic-case-label";
+          return match + VIRUS_CASE_HOOK;
         });
       }
       if (FALLBACK_RENDER_RE.test(patched)) {
@@ -4692,7 +4698,10 @@
         foodPatchMode,
         fallbackFoodPatched,
         virusBranchSeen,
+        exactVirusBranchSeen,
+        virusCaseSeen,
         virusPatched,
+        virusPatchMode,
         fallbackVirusPatched
       };
       state.lastPatchResult = result;

diff -ruN '--exclude=node_modules' blobfix-orig/Blobgameio-extension-BETA-main/src/cellColors/pageVirusPelletColorsBootstrap.js blobfix-work/Blobgameio-extension-BETA-main/src/cellColors/pageVirusPelletColorsBootstrap.js
--- blobfix-orig/Blobgameio-extension-BETA-main/src/cellColors/pageVirusPelletColorsBootstrap.js	2026-07-06 19:26:04.000000000 +0000
+++ blobfix-work/Blobgameio-extension-BETA-main/src/cellColors/pageVirusPelletColorsBootstrap.js	2026-07-06 19:55:51.010519172 +0000
@@ -4,6 +4,8 @@
 const FOOD_CASE_RE = /case 2:case 5:case 0:/;
 const FOOD_BRANCH_HOOK = 'if(g.c&&g.c.M==2&&$wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.food(g)}';
 const VIRUS_BRANCH_RE = /case 4:case 3:if\(g\.q\)\{if\(g\.P\)\{h=g\.P;([$A-Za-z_][$\w]*)\(\);([$A-Za-z_][$\w]*)\(a\.c,g\.K\);([$A-Za-z_][$\w]*)\(a\.c,h,g\.R-g\.M,g\.S-g\.M,g\.N,g\.N\)\}\}else\{\1\(\);\2\(a\.c,g\.K\);\3\(a\.c,(\(?a\.[$A-Za-z_][$\w]*\)?),g\.R-g\.M,g\.S-g\.M,g\.N,g\.N\)\}break;/;
+const VIRUS_CASE_RE = /case 4:case 3:/;
+const VIRUS_CASE_HOOK = 'if(g&&g.K&&$wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.virus(g)}';
 const FALLBACK_RENDER_RE = /function ([$A-Za-z_][$\w]*)\(a,b\)\{var c;if\(b\.q\)\{c=b\.P;([$A-Za-z_][$\w]*)\(\);([$A-Za-z_][$\w]*)\(a\.c,b\.K\);([$A-Za-z_][$\w]*)\(a\.c,c,b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}else if\(b\.P\)\{\3\(a\.c,b\.K\);\4\(a\.c,b\.P,b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}else\{b\.K\.a=0\.75;\3\(a\.c,b\.K\);\4\(a\.c,([$A-Za-z_][$\w]*),b\.R-b\.M,b\.S-b\.M,b\.N,b\.N\)\}\}/;
 
 const PAGE_HOOK = '__BlobCellColorizer';
@@ -710,9 +712,12 @@
     const foodDrawSeen = FOOD_DRAW_RE.test(patched);
     const foodAlphaSeen = FOOD_ALPHA_RE.test(patched);
     let virusPatched = false;
+    let virusPatchMode = 'none';
     let fallbackFoodPatched = false;
     let fallbackVirusPatched = false;
-    const virusBranchSeen = VIRUS_BRANCH_RE.test(patched);
+    const exactVirusBranchSeen = VIRUS_BRANCH_RE.test(patched);
+    const virusCaseSeen = VIRUS_CASE_RE.test(patched);
+    const virusBranchSeen = exactVirusBranchSeen || virusCaseSeen;
 
     if (!patched.includes('$wnd.__BlobCellColorizer.foodBranch(g)') && foodDrawSeen) {
       patched = patched.replace(FOOD_DRAW_RE, (match, setColor, drawRegion, foodTexture) => {
@@ -736,13 +741,11 @@
       });
     }
 
-    if (virusBranchSeen) {
-      patched = patched.replace(VIRUS_BRANCH_RE, (match, initDrawState, setColor, drawRegion, virusTexture) => {
+    if (!patched.includes('$wnd.__BlobCellColorizer.virus(g)') && virusCaseSeen) {
+      patched = patched.replace(VIRUS_CASE_RE, (match) => {
         virusPatched = true;
-        return 'case 4:case 3:'
-          + `if($wnd.__BlobCellColorizer){$wnd.__BlobCellColorizer.virus(g)}`
-          + `if(g.q){if(g.P){h=g.P;${initDrawState}();${setColor}(a.c,g.K);${drawRegion}(a.c,h,g.R-g.M,g.S-g.M,g.N,g.N)}}`
-          + `else{${initDrawState}();${setColor}(a.c,g.K);${drawRegion}(a.c,${virusTexture},g.R-g.M,g.S-g.M,g.N,g.N)}break;`;
+        virusPatchMode = exactVirusBranchSeen ? 'exact-case-label' : 'generic-case-label';
+        return match + VIRUS_CASE_HOOK;
       });
     }
 
@@ -767,7 +770,10 @@
       foodPatchMode,
       fallbackFoodPatched,
       virusBranchSeen,
+      exactVirusBranchSeen,
+      virusCaseSeen,
       virusPatched,
+      virusPatchMode,
       fallbackVirusPatched,
     };
     state.lastPatchResult = result;
diff -ruN '--exclude=node_modules' blobfix-orig/Blobgameio-extension-BETA-main/src/cellRing/pageCellRingBootstrap.js blobfix-work/Blobgameio-extension-BETA-main/src/cellRing/pageCellRingBootstrap.js
--- blobfix-orig/Blobgameio-extension-BETA-main/src/cellRing/pageCellRingBootstrap.js	2026-07-06 19:26:04.000000000 +0000
+++ blobfix-work/Blobgameio-extension-BETA-main/src/cellRing/pageCellRingBootstrap.js	2026-07-06 19:55:51.010024927 +0000
@@ -255,9 +255,9 @@
       state.markers.normalColorMarks += 1;
     }
     return {
-      d: skin ? 1 : color.d,
-      c: skin ? 1 : color.c,
-      b: skin ? 1 : color.b,
+      d: color.d,
+      c: color.c,
+      b: color.b,
       a: skin ? SELF_SKIN_MARK_ALPHA : SELF_MARK_ALPHA,
     };
   }
@@ -970,7 +970,7 @@
       '  bool blobioGlowCell = blobioSelfFull || blobioSelfSkin;',
       '  if (blobioGlowCell) {',
       '    if (blobioSelfSkin) { blobioTint.rgb = vec3(1.0); }',
-      '    blobioTint.a = blobioSelfSkin ? blobioTex.a * blobioCellAlpha : blobioDefaultAlpha;',
+      '    blobioTint.a = blobioSelfSkin ? blobioTex.a : blobioDefaultAlpha;',
       '    gl_FragColor = vec4(blobioTint.rgb * blobioTex.rgb, blobioTint.a);',
       '  } else {',
       '    gl_FragColor = blobioTint * blobioTex;',
@@ -1040,11 +1040,11 @@
     );
     patched = patched.replace(
       /\* texColor \* v_color;/,
-      '* texColor * (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color);',
+      '* texColor * (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, blobioSelfSkin ? 1.0 : clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color);',
     );
     patched = patched.replace(
       /\* v_color \* texColor;/,
-      '* (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color) * texColor;',
+      '* (blobioGlowCell ? vec4(blobioSelfSkin ? vec3(1.0) : v_color.rgb, blobioSelfSkin ? 1.0 : clamp(u_blobioGlowTransparent, 0.0, 1.0)) : v_color) * texColor;',
     );
 
     if (patched !== source) {
